; The Claude Agent SDK peer-requires zod@4 while the legacy Vercel AI SDK v4
; (used only by the older canvas orchestrator) peer-requires zod@3. The in-house
; pipeline runs on the Agent SDK, so we standardise on zod@4 and relax peer
; enforcement for the legacy AI-SDK packages until they are upgraded to v5+.
legacy-peer-deps=true
